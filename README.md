# Hello World

This is an application that displays a flashy message in a window using JavaFX. The contents of the message is specified by the user. 

## Prerequisites

Install the following:

- Java JDK (version 21 or higher)
- Maven

## Usage

Run the application with:

```
mvn javafx:run
```
